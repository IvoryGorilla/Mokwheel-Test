
  <div class="affirm-program-payments-container">
    <div class="container">
        <div class="title">{{section.settings.title}}</div>
        <div class="item-list">
          {% for item in section.blocks %}
            <div class="item">
              <div class="title">{{item.settings.title}}</div>
              <div class="description">{{item.settings.description}}</div>
            </div>
          {% endfor %}
        </div>
    </div>
  </div>

  <script>
    (()=>{

    })()
  </script>

  {% schema %}
  {
  "name": "affirm-program-payments",
  "class": "affirm-program-payments",
  "presets": [{
    "name": "affirm-program-payments",
    "settings": {
  
    }
  }],
  "blocks": [{
    "type": "mega_menu", 
    "name": "Mega menu", 
    "settings":[{
      "type": "richtext",
      "id": "title",
      "label": "title" 
    },{
      "type": "richtext",
      "id": "description",
      "label": "description" 
    }]
  }],
  "settings": [{
    "type": "text",
    "id": "title",
    "label": "title" 
  }]
  }
  {% endschema %}


  <style>

  </style>

  {{ 'affirm-program-payments.css' | asset_url | stylesheet_tag }}
