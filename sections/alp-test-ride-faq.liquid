{% schema %}
{
  "name": "Alp Test Ride FAQ",
  "class": "test-ride-faq-section",
  "settings": [
    {
      "type": "text",
      "id": "faqtitle",
      "label": "Title"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#000000"
    },
    {
      "type": "select",
      "id": "container",
      "label": "Layout",
      "default": "alp-container",
      "options": [
        {
          "value": "alp-container",
          "label": "Default"
        },
        {
          "value": "alp-container-fluid",
          "label": "Fluid container"
        },
        {
          "value": "alp-full-width",
          "label": "Full width"
        }
      ]
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding top",
      "default": 10,
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "em"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding bottom",
      "default": 10,
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "em"
    },
    {
      "type": "checkbox",
      "id": "reset_spacing",
      "label": "Remove default space between sections",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "item_tab",
      "name": "Item Card",
      "settings": [
        {
          "type": "text",
          "id": "faqs_question",
          "label": "Faqs Question",
          "default": "Specifications"
        },
        {
          "type": "richtext",
          "id": "faqs_answer",
          "label": "Faqs Answer"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Alp Test Ride FAQ"
    }
  ]
}
{% endschema %}

{% liquid
  assign st = section.settings
  assign container = st.container
  assign reset_spacing = ''
  if st.reset_spacing
    assign reset_spacing = ' remove_spacing'
  endif
%}

{%- capture style -%}
    --bg-color: {{ st.bg_color }};--text-color: {{ st.text_color }};--padding-top: {{ st.padding_top }};--padding-bottom: {{ st.padding_bottom }};
{%- endcapture -%}

{% style %}
  .arrow-up {
    display: none;
  }

  .help-faq-content {
    margin-top: calc(var(--alp-gutter-y) * 0.75);
  }
  .help-faq-item {
    border-bottom: 1px solid #ececec;
  }
  .help-faq-question p {
    margin: 0;
  }
  .help-faq-answer p {
    margin-top: 0;
  }
  .help-faq-item .help-faq-question {
    display: flex;
    align-items: center;
    line-height: 1.2;
    font-size: clamp(16px, 2vw, 18px);
    justify-content: space-between;
    color: #1a1a1a;
    cursor: pointer;
    padding: 1em 6px 1em 0;
    font-weight: 500;
  }

  .help-faq-answer {
    display: none;
    font-size: 16px;
    font-weight: 400;
    color: #1a1a1a;
    margin-bottom: calc(var(--alp-gutter-y) * 1);
    line-height: 1.65;
    opacity: 0.75;
  }
  .help-faq-bottom {
    border-bottom: 1px solid #ececec;
  }

  .help-faq-all {
    padding: 64px 48px;
    background: #f8f8f8;
    border-radius: var(--alp-border-radius);
  }

  .help-faq-item .help-faq-question {
    font-weight: 600;
  }

  span.arrow-down svg,
  span.arrow-up svg {
    width: 16px;
    height: 16px;
    margin-left: 1em;
  }

  .test-ride-faq .help-faq-item .help-faq-question {
    justify-content: flex-start;
    gap: 12px;
    padding: 24px 0;
  }

  span.faq-arrow {
    display: block;
    margin: -4px 0;
  }
  span.faq-arrow svg {
    color: #aaaaaa;
    display: block;
    transition: 0.2s;
  }
  .help-faq-item.active span.faq-arrow svg {
    transform: rotate(180deg);
  }
  .test-ride-faq .help-faq-answer {
    padding-left: 36px;
    color: #767676;
    opacity: 1;
    margin-bottom: 24px;
  }
  .help-faq-item-title.headline__h6l {
    font-weight: 600;
    margin: 0;
  }
  .test-ride-faq .help-faq-all {
    padding: 40px 60px;
    border-radius: var(--alp-border-radius);
  }
  .help-faq-item.active {
    border-color: #1a1a1a;
  }

  @media (max-width: 750px) {
    .test-ride-faq .help-faq-all {
      padding: 20px;
    }

    .help-faq-content {
      margin-top: 0;
    }
    .help-faq-item-title.headline__h6l {
      font-size: 18px;
    }
  }
{% endstyle %}

<div class="alp-section {{ reset_spacing }}" style="{{ style }}">
  <div class="alp-section-container test-ride-faq page-width {{ container }}">
    <div class="help-faq-all">
      <h2 class="help-faq-title headline__t2">
        {{ st.faqtitle }}
      </h2>
      <div class="help-faq-content">
        <div class="help-faq-box">
          <div class="help-faq-left">
            {%- for block in section.blocks -%}
              {% liquid
                assign bs = block.settings
              %}
              <div class="help-faq-item">
                <h3 class="help-faq-question">
                  <span class="faq-arrow">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24">
                      <path fill="currentColor" d="m7 10l5 5l5-5z" />
                    </svg>
                  </span>
                  <div class="help-faq-item-title headline__h6l">
                    {{ bs.faqs_question -}}
                  </div>
                </h3>
                <div class="help-faq-answer headline__p4">{{ bs.faqs_answer }}</div>
              </div>
            {%- endfor -%}
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    $('.help-faq-item:nth-child(1) .help-faq-answer').css('display', 'block');
    $('.help-faq-item:nth-child(1)').addClass('active');

    $('.help-faq-question').on('click', function (e) {
      if ($(this).parent().find('.help-faq-answer').css('display') == 'none') {
        $(this).parent().addClass('active');
        $(this).parent().find('.help-faq-answer').stop().slideDown(200);
      } else {
        $(this).parent().removeClass('active');
        $(this).parent().find('.help-faq-answer').stop().slideUp(200);
      }
    });
  });
</script>
